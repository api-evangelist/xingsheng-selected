# Xingsheng Selected (兴盛优选)

> Xingsheng Selected — Hunan Xingsheng Youxuan E-Commerce Co., Ltd. — is a Changsha-based
> Chinese community group-buying platform running a "pre-sale plus self-pickup" model:
> consumers order groceries and daily necessities online by 23:00 and collect them the next
> morning from a neighborhood convenience store, supplied through the company's own
> central-warehouse / grid-station / store logistics network.

This file was generated by API Evangelist from the public profile in this repository.
It is NOT published by Xingsheng Selected. Nothing here is an API contract.

generated: 2026-09-04
method: generated
source: apis.yml + repository artifacts

## API surface

Xingsheng Selected publishes **no public API surface**. As of 2026-09-04, probing every host
the company is known to control found no developer portal, no API reference, no OpenAPI,
AsyncAPI, GraphQL or Postman artifact, no SDK or package in any public registry, no MCP
server, no A2A agent card, and no `/.well-known/` document.

- Corporate site: https://xsyxsc.com/ — a Chinese-language Vue single-page application
  covering the business model, supplier / store / service-manager onboarding, news and
  careers. Every `/.well-known/*` path returns the same 102,844-byte SPA shell, which is a
  catch-all response and not a document.
- Supplier system: https://gys.xsyxsc.com/ — "兴盛优选供应商系统", a login-walled internal
  application. No API reference or machine-readable contract is published for it.
- Application backend: https://mall.xsyxsc.com/ — a live JSON host (Spring Boot error
  envelope) serving the consumer product. Every conventional specification path
  (`/v2/api-docs`, `/v3/api-docs`, `/openapi.json`, `/swagger.json`, `/api-docs`,
  `/swagger-resources`, `/doc.html`, `/actuator`) returns HTTP 404. It is an undocumented
  first-party app backend, not a published API.

## Partner and supplier onboarding (not APIs)

- 供应商招募 / supplier recruitment, 门店入驻 / store onboarding, 平台入驻 / platform
  onboarding, and 服务经理招募 / service-manager recruitment are web forms and a hotline
  (400-888-9997) on https://xsyxsc.com/, not developer programs.

## Public packages (not API clients)

The company's front-end platform team publishes 121 public npm packages under the `@xsyx`
scope — the "hanzo" H5 / WeChat mini-program component library behind the consumer app, a
"jarvis" JSON-Schema render engine, an error-reporting SDK and CLI tooling. They are internal
platform libraries, not clients for a public API, and the scope has had no release since
2021-03-11. See packages/xingsheng-selected-packages.yml.

## Artifacts in this profile

- packages/xingsheng-selected-packages.yml — 121 first-party npm packages, with versions and dates
- security/xingsheng-selected-domain-security.yml — probed TLS/HSTS/DNSSEC/CAA/SPF/DMARC posture
- well-known/xingsheng-selected-well-known.yml — recorded absence of every probed well-known document

## Corrections

If Xingsheng Selected publishes an API, specification or developer portal that this profile
missed, open an issue on https://github.com/api-evangelist/xingsheng-selected or email
info@apievangelist.com and the profile will be re-run.
